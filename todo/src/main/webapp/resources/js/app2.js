/*  "petSelect"   -> [ ]
	fetch, $.ajax
     리턴결과 건수만큼 반복 실행
        - petTemplated 클래스 태그를 복사
		- title, breed, age, location등 수정
		- petsRow 아디를 가진 태그에 추가
*/
fetch("petSelct").
then( json);
then(result=>{
	for() {}
}).then(adotptSelect )

function adotptSelect() {
fetch("adoptSelct").
then();
}

